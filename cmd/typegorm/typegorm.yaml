database:
  dialect: "mysql"
  dsn: "root:password@tcp(127.0.0.1:3306)/my_app_db?charset=utf8mb4&parseTime=True&loc=Local" # MUST UPDATE
  pool:
    maxIdleConns: 5
    maxOpenConns: 20
    connMaxLifetime: "30m" # e.g., 30 minutes
    connMaxIdleTime: "10m" # NEW: e.g., 10 minutes idle timeout

migration:
  directory: "./db/migrations"
  tableName: "typegorm_schema_history"